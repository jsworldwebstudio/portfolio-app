import React, { Fragment } from 'react';
import Typography from '@material-ui/core/Typography';

const Resume = () => {
  return (
    <Fragment>
    <Typography variant="headline" color="textSecondary">
      Resume Component Loaded...
    </Typography>
    </Fragment>
  );
}

export default Resume;
